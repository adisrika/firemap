# .env.example — copy to .env.local for local development
# NEVER commit .env.local to version control

# The canonical URL of the site (no trailing slash)
# On Vercel: set in Project Settings > Environment Variables
NEXT_PUBLIC_SITE_URL=https://firemap.app

# Phase 2: Plausible Analytics domain
# Register at plausible.io, then set to your domain
# Leave blank/commented to disable analytics in development
# NEXT_PUBLIC_PLAUSIBLE_DOMAIN=firemap.app
